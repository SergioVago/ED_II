#if !defined(INSERTION_SORT_H_)
#define INSERTION_SORT_H_

// Uses the insertion sort in ascending order
// Needs the pointer to the array and his size
void InsertionSort(int *array, int size);

// Print an array
// Needs the pointer to the array and his size
void ArrayPrint(int *array, int size);

#endif // INSERTION_SORT_H_
